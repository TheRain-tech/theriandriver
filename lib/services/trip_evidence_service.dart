class TripEvidenceService {
  const TripEvidenceService();

  Future<void> prepareTripEvidenceSession(String rideId) async {
    // Future boundary for camera/photo/audio evidence. No permissions are
    // requested until a real evidence feature is implemented.
  }
}
